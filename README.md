# Cardano Savings Vault
A fully functional Cardano DApp for long-term savings goals.

## Deployment:
- **Platform**: Vercel
- **Framework**: Next.js 14 (App Router)
- **Library**: Mesh SDK (Cardano)

Ensure the root directory in Vercel is set to `./` where the `package.json` is located.
